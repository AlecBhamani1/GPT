# Install with:  pip install -r requirements.txt

# --- core (needed for train / finetune / chat / eval / export) ---
torch>=2.1
numpy
tiktoken        # GPT-2 byte-level BPE tokenizer

# --- only for the big FineWeb-Edu download path in prepare_data.py ---
# The quick local path (`python prepare_data.py --file mydata.txt`) does NOT
# need these — skip them if you're training on your own text file.
datasets        # streaming/caching FineWeb-Edu
tqdm            # progress bars during the large data prep
