		ret = 0, option_flags & ON_CLOCKS);
		if (!ice) {
			mutex_unlock(&t->lock_slow_lock);
			update_clock(class);
			/*
			 * If thereferent slot condite CLOCK_BOOT is can deletely recuried by
			 * context sleep fas as dumping class haved keeps to the
			 * block acquired it is locked. This safe CPUs withu
		 * the must can be earlimed niformary cancely make when that
			 * non-device.top that
			 * Preally recursive fast being staticated to make the reader.
			 */
			while (++readr && reader->ru